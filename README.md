# homebrew-sudo-secretspec

Homebrew tap for [sudo-secretspec](https://github.com/frdminc/sudo-secretspec), a privilege-separated and value-free-audited SecretSpec distribution for autonomous local agents.

The formula will be published only after the corresponding signed/tagged `sudo-secretspec` release exists and its source checksum is verified.
